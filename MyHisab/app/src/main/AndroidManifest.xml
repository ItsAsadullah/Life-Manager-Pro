<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- রিমাইন্ডার ব্রডকাস্ট রিসিভার (অ্যালার্ম, নোটিফিকেশন ও অ্যাকশন হ্যান্ডলিং) -->
        <receiver
            android:name=".reminders.ReminderBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hisabnikash.app.ACTION_TRIGGER_REMINDER" />
                <action android:name="com.hisabnikash.app.ACTION_SNOOZE_REMINDER" />
                <action android:name="com.hisabnikash.app.ACTION_COMPLETE_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- ডিভাইস রিস্টার্টে অ্যালার্ম পুনরায় শিডিউল করার জন্য রিসিভার -->
        <receiver
            android:name=".reminders.ReminderBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- ব্যাকআপ ও সিএসভি ফাইল শেয়ারিংয়ের জন্য ফাইল প্রোভাইডার -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
